#############################################################
#############################################################
##
##  Excludes for config files with potentially sensitive info
##
#############################################################
#############################################################
# Environment files for podman compose
*.env
# Site-specific Ansible config files
.inv.*.yml
site_data
site_data.yml
vaultpw
.vaultpw
**/ansible/*-vars.yml
**/ansible/*.vars.yml

# Site-specific Terraform config files
# Exclude all .tfvars files, which are likely to contain sensitive data, such as
# password, private keys, and other secrets. These should not be part of version
# control as they are data points which are potentially sensitive and subject
# to change depending on the environment.
*.tfvars
*.tfvars.json

# Ignore override files as they are usually used to override resources locally and so
# are not checked in
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# Ignore CLI configuration files
.terraformrc
terraform.rc

#############################################################
#############################################################
##
##  Excludes for files generated by Terraform (OpenTofu)
##
#############################################################
#############################################################
# Terraform state and lock files
.terraform/
.terraform.lock.hcl
.terraform.tfstate.lock.info
terraform-plugin-proxmox.log
terraform.tfstate
terraform.tfstate*backup
**/.terraform/*
.terraform.lock.hcl
# .tfstate files
*.tfstate
*.tfstate.*

# Proxmox provider log
terraform-plugin-proxmox.log
terraform.log

# Crash log files
crash.log
crash.*.log

# Terraform generated Ansible files
**/ansible.cfg


#############################################################
#############################################################
##
##  Excludes for files generated by Ansible
##
#############################################################
#############################################################
# Ansible retry files
*.retry
# Ansible generated Kubernetes config files
**/.kube/*

#############################################################
#############################################################
##
##  Misc. temporary files
##
#############################################################
#############################################################
.travis.yml
*.swp
